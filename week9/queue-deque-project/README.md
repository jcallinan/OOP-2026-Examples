# Queue and Deque Chapter Code (Week 9)

This project contains the code snippets extracted from:

- `week9/5 Queues and Deques.pdf`

Included snippets:

- `Queue` class and usage example
- `Deque` class
- `BrowserHistory` deque usage example
- `CircularQueue` class
- `hotPotato` simulation
- `isPalindrome` checker
- TypeScript `countStudents` solution snippet

## Run examples

```bash
npm run example:all
npm run example:queue
npm run example:deque
npm run example:browser-history
npm run example:hot-potato
npm run example:palindrome
npm run example:count-students
```
